datasource db {
  provider = "postgresql"
}

model Incident {
  id         String   @id @default(uuid())
  type       String
  severity   String
  platform   String
  content    String
  detectedAt DateTime @default(now())
  status     String   @default("pending")
  confidence Float
  sourceUrl  String?
}
